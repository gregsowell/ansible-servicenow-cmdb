---
plugin: servicenow.servicenow.now
instance: "{{ snow_instance }}"
username: "{{ snow_username }}"
password: "{{ snow_password }}"
table: cmdb_ci_ip_switch
fields: [ip_address,name,manufacturer]
keyed_groups:
  - key: manufacturer | lower
    prefix: ''
    separator: ''
